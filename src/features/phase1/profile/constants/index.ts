/**
 * 📋 Profile Constants - Main Export
 *
 * Centralized export for all profile-related constants
 */

// Profile Fields Configuration
export {
  FIELDS_BY_SECTION,
  FIELD_VALIDATION_RULES,
  PROFILE_FIELDS,
  PROFILE_SECTIONS,
  REQUIRED_FIELDS_BY_ROLE,
} from './profileFields';

// Profile Templates Configuration
export { PROFILE_COMPLETION_GUIDANCE, PROFILE_TEMPLATES } from './profileTemplates';

// Note: Types are defined in profileTemplates.ts but not exported
// If needed, they can be imported directly from the file
