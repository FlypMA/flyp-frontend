/**
 * URL Generator Service - Compatibility Layer
 * 
 * Re-exports the URL generator service from the shared services
 * for backward compatibility with existing import paths.
 */

export { default } from '../../../shared/services/urls/urlGenerator';
export { default as UrlGenerator } from '../../../shared/services/urls/urlGenerator';
