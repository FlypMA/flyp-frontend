/**
 * User Consolidated Types - Compatibility Layer
 * 
 * Re-exports user types from the shared types directory
 * for backward compatibility with existing import paths.
 */

export * from '../shared/types/user';
export { User as default } from '../shared/types/user';
